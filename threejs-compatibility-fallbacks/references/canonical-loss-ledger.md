# Canonical Loss Ledger

Use this ledger only after the user explicitly asks how to apply fallback when
WebGPU is unavailable. The canonical owner remains authoritative; this file
records what may be weakened, what must never be faked, and what evidence must
replace canonical proof.

Pending gate on the creature rows: the numeric thresholds and tier constants
they cite (`< 0.02` residual, `K` per tier, the `seek(t)` equality) are the
creature skill's lab-gate contract; the lab is under construction in-tree
(`threejs-procedural-creatures/examples/webgpu-procedural-creature-lab`,
`HANDOFF.md` §3 item 3.9e) and has not yet run its full gate table green.
Until that run is recorded, "Preserved tiers rerun tests" is a forward
obligation — no creature row may cite passed evidence today.

| owner skill | canonical invariant | permitted downgrade | forbidden fake | validation delta |
| --- | --- | --- | --- | --- |
| threejs-spectral-ocean | FFT cascades use real spectra, Hermitian packing, frequency-space derivatives, and foam history. | Fewer cascades, smaller grids, precomputed displacement/foam loops. | Gerstner piles presented as spectral FFT water. | DC/one-bin/derivative tests still pass or are marked removed; screenshots name lost foam dynamics. |
| threejs-procedural-vegetation | Rooted instanced vegetation preserves density fields, species shape, culling, and wind roots. | Lower density, static wind textures, baked placement maps, impostors. | Sliding roots, uncullable monoliths, or random card scatter called species growth. | Patch counts, root anchoring, seed replay, and culling bounds are rechecked. |
| threejs-procedural-creatures | Mandatory Architecture / Field, Shell, Shading, and Performance Budgets: SDF blend-shell skinning evaluates the bounded candidate field in the TSL vertex stage, snaps the shell to `d = iso` with Newton steps, keeps analytic-gradient normals/colors on the same candidate set, and gates residual `< 0.02` of body scale. | Preserved: WebGPU-budget fallback only moves down the skill's canonical tiers (Hero 12 radial/3 cap rings, 2 + early-out snap, K=8; Crowd 10 radial/2 cap rings, 1-2 snap, K=4-6; Background 8 radial/2 cap rings, 1 snap, K=2-4). Weakened: precomputed/static or CPU/offline tiers replace live snap with baked morph or skeletal shell approximations generated from canonical poses; live iso-surface response, field-gradient self-AO, and exact shadow/visible snapped-position parity are lost. Removed: legacy or feature-removal tier uses generated mesh or canned skinned approximation and must not claim SDF residual, candidate-set parity, or the `E_vertex = (snapSteps + 1) * K` evaluation contract. | Baked morphs, finite-difference shader normals, or a smooth mesh called SDF blend-shell skinning. Masked full-budget slot loops, shadow paths with different positions, or finite differences presented as the analytic-gradient path. | Preserved tiers keep CPU/TSL parity, residual, candidate K, silhouette-vs-shadow, and tier shell gates. Weakened tiers record the bake source pose range and mark snap residual, live self-AO, and dynamic shadow parity as weakened or removed. Removed tiers show final and diagnostic captures labelled as generated or skeletal, not SDF snapped. |
| threejs-procedural-creatures | Mandatory Architecture / Rendering at scale and Performance Budgets: per-creature pose lives in storage indexed by `instanceIndex * maxParts + slot`, with one shared geometry/material per species/tier, one contiguous pose write per frame, and one draw per species/tier within the skill's target population budgets. | Preserved: WebGPU-budget fallback lowers population/species/tier pressure within the skill's own Desktop discrete, Desktop integrated, or Mobile WebGPU target ranges while keeping storage pose indexing, variant caching, real per-instance bounds, and one draw per species/tier. Weakened: no-storage fallback replaces storage-buffer pose instancing with CPU pose upload through updated attributes, texture pages, or baked pose pages; batching may split by pose page or tier, upload cost shifts to CPU, and same-species creatures no longer inherit the canonical contiguous-storage proof. Removed: static impostor, baked clip, or canned mesh tiers remove live per-creature pose storage and must label crowd motion as baked. | Per-creature material uniforms, one material per creature, `frustumCulled = false`, or every creature as its own draw call presented as storage-buffer pose instancing. | Preserved tiers recheck draw counts, one contiguous storage write, variant cache keys, allocation counters, and per-instance posed bounds. Weakened tiers record CPU upload path, draw splits, allocation behavior, and lost storage-index proof. Removed tiers validate only the baked crowd artifact and explicitly omit storage-buffer batching claims. |
| threejs-procedural-creatures | Mandatory Architecture / Pose runtime, Locomotion, Lab, and Failure Conditions: planted-foot procedural gait advances on a fixed-step accumulator (`1/60` or `1/120`, clamped input dt), renders interpolated pose, uses world-planted feet with analytic 2-bone IK and a Gram-Schmidt bend hint, and stays deterministic from seeded LCG plus sim clock. | Preserved: WebGPU-budget fallback reduces active actors or locomotion layers but keeps fixed-step simulation, presentation interpolation, O(slots) rig updates, seeded determinism, and the lab equality `seek(t)` = `step(t * 60, 1000/60)`. Weakened: precomputed/static or CPU/offline tiers replace reactive planted gait with baked step curves, morph clips, or canned clips sampled from sim time; terrain/lag reaction, zero stance-drift behavior, and live IK correction are lost or narrowed to the authored clip domain. Removed: feature-removal or legacy tier disables planted-foot IK and names the result as canned locomotion, not procedural planted gait. | Raw render-dt locomotion, wall-clock/random-driven motion, foot sliding hidden by interpolation, or canned clips called reactive planted gait. | Preserved tiers rerun deterministic seek/step, foot-drift markers, seed sweeps, and hitch tests. Weakened tiers label clip-domain limits and mark stance-drift/IK gates as weakened outside authored clips. Removed tiers show final captures with planted-foot and IK evidence omitted rather than claimed. |
| threejs-water-optics | Heightfield waves, shared normals, Fresnel/refraction, and caustics derive from the same water state. | Smaller heightfield, analytic-only waves, static caustic maps. | Normal-only waves with flat geometry called dynamic water. | Height/normal parity, refraction bounds, caustic map color space, and dispose loop are recorded. |
| threejs-volumetric-clouds | Density, transmittance, lighting, shadows, and history rejection share one weather volume. | Lower march resolution, fewer samples, static cloud cards, no shadows. | Same-UV history smear or billboard fog called volumetric reconstruction. | No-post density/debug captures, temporal rejection labels, and lost shadow evidence are named. |
| threejs-black-holes-and-space-effects | Curved rays advance once per step with bounded integration and capped error. | Fewer steps, static starfield distortion, precomputed lens sprites. | Double-advanced rays or unbounded NaNs hidden by bloom. | Reference rays, capped-pixel count, and step-budget screenshots are included. |
| threejs-ambient-contact-shading | AO estimates horizon visibility or built-in GTAO with depth/normal-consistent reconstruction. | Lower resolution, fewer slices, static contact decals. | Darkening creases as a color multiply called AO. | No-post AO contribution, depth/normal diagnostics, and halo checks are rerun. |
| threejs-bloom | Bloom consumes scene-referred HDR energy before the single output transform. | Lower mip count, lower resolution, restricted emissive mask. | Display-space glow or material swaps presented as production selective bloom. | HDR hierarchy, emissive MRT, no-post, and output-owner checks stay attached. |
| threejs-image-pipeline | One pass graph owns gbuffer, tone map, output transform, and effect-local targets. | Drop optional effects or lower pass resolution. | Duplicate gbuffers, duplicate tone mapping, or effect-owned output conversion. | Ownership map, render-target inventory, and color pipeline record are updated. |
| threejs-scalable-real-time-shadows | Light-space visibility uses stable texels, correct bias, and targeted invalidation. | Fewer cascades/tiles, lower update cadence, static baked shadows. | Unsnapped full redraws called cached shadows. | Texel snapping, refresh budget, bias units, and shadow target memory are recorded. |
| threejs-dynamic-surface-effects | Contact history evolves with dt-correct decay and storage ping-pong ownership. | Lower history resolution, slower updates, static mask overlays. | Per-frame constants or tint overlays called temporal accumulation. | Reset/resize/history tests and temporal checkpoints name lost interaction. |
| threejs-rain-snow-and-wet-surfaces | One weather envelope drives precipitation, wetness, ripples, accumulation, and post treatment. | Fewer particles, static wetness maps, no model snow, no splashes. | Separate fake wetness systems drifting from precipitation state. | Shared weather identity, color-space ledger, and material/debug views are included. |
| threejs-sky-atmosphere-and-haze | LUT-factorized Rayleigh/Mie/ozone transmittance and aerial perspective use consistent units. | Coarser LUTs, static sky cubemap, no aerial froxel. | Gradient skyboxes called physical atmosphere. | LUT manifest, unit scale, sky/no-atmosphere/aerial captures name lost scattering. |
| threejs-procedural-planets | Body fields preserve spherical continuity, crater/biome coupling, and CPU/GPU parity. | Lower LOD, baked height/biome textures, disabled close approach. | Plane noise wrapped on a sphere with visible seams. | Seed parity, face-edge continuity, orbit/mid/close captures name the loss. |
| threejs-procedural-fields | One field definition serves vertex, fragment, compute, and bake paths without color-space drift. | Bake fields to data textures, reduce octaves, freeze animation. | CPU/GPU formula drift or sRGB data maps. | Hash/manifest, NoColorSpace map checks, and parity samples are recorded. |
| threejs-procedural-materials | PBR channels preserve energy, normal/roughness coupling, and color/data domains. | Static generated maps, fewer procedural octaves, simplified clearcoat/emission. | Waxy metals, sRGB-as-data, or gray emission called physical material. | Channel validation, NoColorSpace/SRGBColorSpace ledger, and no-post material swatches are rerun. |
| threejs-exposure-color-grading | Exposure meters scene-referred luminance and output is owned once. | Lower cadence, authored shot table, smaller LUT. | CPU readback driving the same frame or grading used to fix bad lighting. | Meter domain, nonblocking readback label, LUT domain, and output owner are recorded. |
| threejs-procedural-geometry | Mesh writers own topology, hard edges, UV density, material groups, and byte budgets. | Simpler profile, fewer segments, baked mesh variants. | Shared vertices across incompatible surfaces or global normals hiding edges. | Vertex/group/UV/normal gates rerun on every generated variant. |
| threejs-particles-trails-and-effects | Effect pools preserve radiometry, dense-swap identity, ballistics, and zero allocation churn. | Lower pool capacity, baked trails, disabled secondary debris. | Random particles and bloom called physical plasma or pooled effects. | Pool invariants, HDR hierarchy, seed replay, and allocation counters are included. |
| threejs-procedural-motion-systems | Motion remains frame-rate independent, deterministic, and quaternion-safe. | Baked timelines, fewer active actors, disabled docking/spin layers. | Per-frame lerp or quaternion flips hidden by camera cuts. | 30/60/120 FPS replay and handoff snapshots are rerun. |
| threejs-camera-controls-and-rigs | Camera owns view/projection, -Z forward, basis fallback, and floating-origin handoff. | Disable modes, shorter path, coarser collision/obstruction. | Manual orbit screenshots pretending to be deterministic camera proof. | Fixed matrices, target projection, forward sign, and replay deltas are recorded. |
| threejs-procedural-buildings-and-cities | Grammar output compiles to material-slot batches with exposed-edge semantics. | Fewer variants/floors/ornaments, baked city blocks. | Per-primitive mesh soup called a compiler. | Draw counts, material groups, edge inventory, and deterministic variant hashes are checked. |
| threejs-visual-validation | Evidence uses fixed contracts, no-post diagnostics, seed/temporal sweeps, timing, and leak loops. | Fewer screenshots or labelled CPU-only timing when GPU timing is absent. | Single final frame as proof. | Manifest explains omitted evidence; malformed bundles still fail validation. |
| threejs-choose-skills | Routing starts from physical causes, canonical owner, backend preflight, and minimal skill set. | Defer optional siblings or block missing capabilities. | Routing "make it beautiful" straight to post effects. | Route manifest names selected/omitted skills, owner boundaries, and blockers. |
